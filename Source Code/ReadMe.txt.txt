In order to run the code, we have to start the servers using SSH and make sure to update the list of serevrs running 
in the file named "ListOfServers.java".

After doing this, we compile all the files and run the file"GladosFile.java".This is the parent file.
and after running it, it will show us the Use of Critical Section.